# Backend Environment Variables (.env)
# Copy this file to .env and fill in your actual values

# === FLASK CORE ===
FLASK_ENV=production
SECRET_KEY=your-super-secret-key-change-this-in-production

# === ADMIN CREDENTIALS ===
# Simple auth: use a strong password in production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-strong-password-here

# === DATABASE ===
# For local dev: sqlite:///gym.db
# For production: use PostgreSQL or MySQL
DATABASE_URL=sqlite:///gym.db

# === FILE UPLOADS ===
# Where to store uploaded files locally (optional if using S3)
UPLOAD_FOLDER=uploads

# === AWS S3 (Optional, for cloud file storage) ===
# Leave blank to use local file storage
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=
AWS_REGION=us-east-1

# === YOUTUBE (Optional, for uploading videos to YouTube) ===
# Requires Google OAuth2 offline access credentials
YT_CLIENT_SECRETS_FILE=/path/to/client_secrets.json
YT_REFRESH_TOKEN=your-refresh-token-here

# === CORS ===
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:5000

# === LOGGING ===
LOG_LEVEL=INFO
