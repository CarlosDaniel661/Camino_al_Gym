# frontend/.env.production
# Variables de entorno para el frontend en PRODUCCIÓN (Vercel)
# Nota: En Vercel, configura esto desde el panel (Settings > Environment Variables)

# URL del backend en producción (cambiar al dominio/URL de tu backend)
REACT_APP_API_URL=https://camino-al-gym-backend.fly.dev

# Opcional: otros valores
REACT_APP_ENV=production
